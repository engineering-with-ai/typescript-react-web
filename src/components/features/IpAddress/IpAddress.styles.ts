import { styled, View } from "@tamagui/core";

/** Container for octet row. */
export const OctetContainer = styled(View, {
  flexDirection: "row",
  gap: "$sm",
});

/** Wrapper for octet with dot. */
export const OctetWrapper = styled(View, {
  flexDirection: "row",
  alignItems: "center",
});
