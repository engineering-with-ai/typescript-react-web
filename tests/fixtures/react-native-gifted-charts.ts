import { View } from "react-native";

/**
 * Mock for react-native-gifted-charts.
 * Actual chart rendering tested in integration tests.
 */
export const LineChart = View;
export const BarChart = View;
